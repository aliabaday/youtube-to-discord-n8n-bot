# 📢 Automatically Announce YouTube Videos on Discord (with n8n)

You can create a simple AI-powered bot in **n8n** to automatically announce your YouTube videos on Discord whenever a new video is uploaded.

---

## 🚀 Overview

When a new video is published on your YouTube channel (or playlist), this workflow:

- Detects the new upload via **RSS**
- Sends an automatic announcement to a **Discord channel**
- Includes the **video title, link, and thumbnail**

---

## 🛠 Step-by-Step Setup

### 1️⃣ Create a New n8n Workflow

- Open **n8n**
- Create a new workflow
- Add an **RSS Feed Trigger** node

#### RSS Settings

- Paste your **YouTube Channel or Playlist RSS feed URL**
- Configure the trigger as follows:
  - **Mode:** Every hour
  - **Minute:** Every 5 minutes

This setup ensures the bot checks efficiently without unnecessary load.

---

### 2️⃣ Create a Discord Webhook

- Open **Discord**
- Go to the channel where you want the announcements
- Click **Edit Channel**
- From the left menu, select **Webhooks**
- Click **New Webhook**
- Copy and save the **Webhook URL**

---

### 3️⃣ Configure the HTTP Request in n8n

- Go back to **n8n**
- Add an **HTTP Request** node

#### HTTP Request Settings

Method: POST
URL: 
<pre><code>https://discord.com/api/webhooks/YOUR_WEBHOOK_URL</code></pre>

⚠️ Replace YOUR_WEBHOOK_URL with your actual Discord webhook URL.

Set the Request Body

Send Body: Enabled

Body Content Type: Form URL Encoded

Specify Body: Using Fields Below

Body Parameters

Name: content

Value:

<pre><code>={{ '🎬 YOUR MESSAGE!\n**' + $json["title"] + '**\n' + $json["link"] }}</code></pre>

You can customize YOUR MESSAGE! (e.g. “New video is live!”).
(e.g. “New video is live!”).

✅ Result

Once everything is set up:

Every new YouTube upload is automatically detected

Discord receives a message with:

🎬 Video title

🔗 Video link

🖼 Thumbnail (auto-generated by Discord)

No manual sharing required anymore 🚀


